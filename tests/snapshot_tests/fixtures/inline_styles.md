---
styles:
  accent:
    bold: true
    color: "#FF6B00"
  subtle:
    italic: true
    color: "#888888"
  code_look:
    monospace: true
    color: "#00FF88"
    background: "#1A1A2E"
---

# Inline Styled Text

This paragraph has ::accent(orange bold text) embedded inline.

You can also use ::subtle(subtle gray italic) spans.

A ::code_look(monospace styled) span with background.

Links with classes: [link.accent](https://example.com) renders bold orange.
